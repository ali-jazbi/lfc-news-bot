# راه‌اندازی تست — مرحله به مرحله

هدف این مرحله: ربات تستی در گروه تستی خودت خبر ترجمه‌شده بگذارد.
**هیچ چیزی روی کانال نمی‌رود.** انتشار دستی و توسط ادمین است.

---

## گام ۱ — ربات تستی بساز (۲ دقیقه)

1. در تلگرام برو به `@BotFather`
2. `/newbot` بزن → یک اسم و یک یوزرنیم (مثلاً `lfc_test_1_bot`) بده
3. توکنی که می‌دهد را جایی ذخیره کن (شبیه `8123456789:AAF...`)

دو تنظیم مهم در BotFather (وگرنه ربات پیام‌های گروه را نمی‌بیند):
- `/setprivacy` → رباتت را انتخاب کن → **Disable**
- `/setjoingroups` → **Enable**

---

## گام ۲ — گروه تستی (۱ دقیقه)

1. یک گروه بساز (یا همونی که داری)
2. ربات تستی را به گروه اضافه کن
3. ربات را **ادمین گروه** کن (برای اینکه بتواند پیام ویرایش کند)

عدد گروه را در گام ۵ می‌گیریم.

---

## گام ۳ — کلید ترجمه رایگان (۲ دقیقه)

1. برو به https://aistudio.google.com/apikey
2. با حساب گوگل وارد شو → **Create API key**
3. کلید را کپی کن (شبیه `AIza...`)

رایگان است و برای این حجم کار کاملاً کافیه. کارت بانکی نمی‌خواهد.

---

## گام ۴ — نصب روی لپ‌تاپ (۲ دقیقه)

فایل zip را اکسترکت کن، ترمینال را در پوشه `lfc-news-bot` باز کن:

```bash
pip install -r requirements.txt
cp .env.example .env        # در ویندوز:  copy .env.example .env
```

حالا فایل `.env` را با Notepad یا VSCode باز کن و فقط این دو خط را پر کن:

```
BOT_TOKEN=8123456789:AAF...        ← توکن گام ۱
GEMINI_API_KEYS=AIza...            ← کلید گام ۳
```

> داخل ایران حتماً پراکسی هم لازم داری وگرنه نه تلگرام نه گوگل جواب نمی‌دهد:
> `PROXY=socks5h://127.0.0.1:1080`  (پورت را از کلاینت خودت ببین)

---

## گام ۵ — گرفتن آیدی گروه (۱ دقیقه)

```bash
python main.py
```

در اولین اجرا خطای "ADMIN_CHAT_ID خالی است" می‌دهد. طبیعیه.
یک راه ساده‌تر: در گروه تستی پیام `/id` بفرست و هم‌زمان این را بزن:

```bash
python scripts/diagnostics/get_chat_id.py
```

عددی مثل `-1002345678901` می‌دهد. آن را در `.env` بگذار:

```
ADMIN_CHAT_ID=-1002345678901
```

---

## گام ۶ — تست اول: بدون تلگرام

اول ببین ترجمه درست کار می‌کند:

```bash
python main.py --sample --dry-run
```

باید دو خبر ترجمه‌شده فارسی در ترمینال ببینی.
✅ اگر دیدی → کلید Gemini و ترجمه سالم است.

---

## گام ۷ — تست دوم: در گروه تستی

```bash
python main.py --sample
```

باید در گروه دو پست با قالب کانال و سه دکمه ببینی:

```
📤 نسخه آماده انتشار   |   ❌ رد
🔄 ترجمه مجدد
```

**این خبر نمونه است و همیشه جواب می‌دهد** — حتی وقتی هیچ خبر تازه‌ای در دنیا نیست.

---

## گام ۸ — تست دکمه‌ها

دکمه‌ها وقتی کار می‌کنند که ربات **روشن باشد**. پس:

```bash
python main.py
```

رهایش کن روشن بماند، بعد در گروه:

| دستور/دکمه | اتفاقی که می‌افتد |
|---|---|
| `/sample` | یک خبر نمونه می‌فرستد (تست هر وقت خواستی) |
| `/check` | همین الان منابع واقعی را چک می‌کند |
| `/status` | وضعیت ربات |
| `/id` | آیدی گروه |
| 📤 نسخه آماده انتشار | یک پیام تمیز (بدون دکمه و لینک منبع) در همان گروه می‌گذارد → ادمین فورواردش می‌کند به کانال |
| ❌ رد | خبر رد می‌شود |
| 🔄 ترجمه مجدد | دوباره ترجمه و همان پیام را ویرایش می‌کند |

---

## گام ۹ — تست با خبر واقعی

```bash
python main.py --test
```

`--test` یعنی فیلتر تکراری خاموش است، پس حتی خبرهای دیروز را هم می‌فرستد و می‌توانی هرچند بار تکرار کنی.

اگر خروجی گفت `جمع‌آوری شد: 0 آیتم`، لاگ را نگاه کن:
- `src.lfc` خطا داد → ساختار سایت باشگاه عوض شده (خودکار فال‌بک می‌زند)
- `src.romano` گفت هیچ فیدی جواب نداد → طبیعی است، اینستنس‌های عمومی ناپایدارند

لاگ را بفرست تا درستش کنم. برای تست فقط سایت باشگاه می‌توانی در `.env` بگذاری `ENABLE_ROMANO=false`.

---

## خلاصه دستورات

| دستور | کار |
|---|---|
| `python main.py --sample --dry-run` | تست ترجمه، بدون تلگرام |
| `python main.py --sample` | خبر نمونه در گروه |
| `python main.py --test` | خبر واقعی، بدون فیلتر تکراری |
| `python main.py --once` | یک سیکل عادی |
| `python main.py` | سرویس دائمی + دکمه‌ها فعال |
| `python scripts/diagnostics/get_chat_id.py` | گرفتن آیدی گروه |

---

## مرحله بعد (فعلاً لازم نیست)

وقتی از کیفیت ترجمه راضی بودی:
1. گلاسری `glossary.json` را کامل کن (مهم‌ترین عامل کیفیت)
2. ربات واقعی و گروه واقعی ادمین‌ها را جایگزین کن
3. دیپلوی روی سرور رایگان (Oracle Cloud) تا ۲۴ ساعته روشن باشد
4. اگر خواستند ربات خودش روی کانال بفرستد: `PUBLISH_MODE=auto` + `CHANNEL_ID=@...`
