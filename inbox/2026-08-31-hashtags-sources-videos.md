---
type: Session
engine: cline
date: 2026-08-31
---

# Session — هشتگ، منبع چندگانه، چند ویدیو

## Done
- **هشتگ‌ها از خروجی ترجمه حذف شدند** (`_strip_hashtags` در translate.py، هر دو
  مسیر LLM و گوگل) + پاک‌سازی artifact `\x3C`. متن انگلیسیِ اصلی دست‌نخورده —
  توییت با هشتگ #LFC/#Liverpool همچنان مرتبط شناسایی می‌شود (تست regression).
- **منبع‌یابی چندگانه**: `detect_original_sources` جدید — اسکن کل متن برای
  `@handle` و `_handle` (قرارداد «به نقل از _pauljoyce») + لینک‌های نیتر، dedupe،
  حذف نویسنده. اولین = منبع اصلی (کانال)، بقیه در یادداشت ادمین («منابع دیگر»).
  استراتژی تصویب‌شده: هر منشن = منبع (کیس Santi_J_FM حل شد).
- **چند ویدیو**: یوزربات قبلاً فقط اولین ویدیوی reply از @twittervid_bot را
  forward می‌کرد — حالا همه‌ی ویدیوهای reply در پنجره‌ی کوتاه (1.5s) جمع و
  forward می‌شوند (کپشن روی اولی). مسیر لوکال هم همه‌ی `item["video_urls"]` را
  جدا می‌فرستد (سقف `TWITTER_VIDEO_MAX`=4، جدید در config + .env.example).
- پروتکل TDD رعایت شد: هر رفتار اول FAIL، بعد فیکس، بعد PASS.
- کل سوییت: 182 پاس (بدون رگرسیون).

## Decided
- هر @منشن = منبع (اختیار ادمین در پیش‌نمایش) — اگر بعداً false positive اذیت
  کرد، می‌شود فلگ whitelist به config اضافه کرد.
- چند ویدیو: هر ویدیو پیام جدا (مثل رفتار @twittervid_bot) — آلبوم میکس
  photo/video پیاده نشد (API تلگرام برای آپلود آلبوم ویدیویی سنگین‌تر است).

## Next
- تست زنده: توییت دو-ویدیویی، توییت PhysioScout (هشتگ)، توییت anfieldsociaI
  (منبع Santi_J_FM).
