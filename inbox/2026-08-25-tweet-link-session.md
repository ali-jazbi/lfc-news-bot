---
type: Session
engine: claude-code (ox-alpha)
date: 2026-08-25
---

# Session — قابلیت لینک خام توییت در گروه ادمین

## Done
- کشف کلیدی: صفحه‌ی status تک‌توییت x.com همان ساختار relay پروفایل را دارد → پارسر موجود `sources/xscrape.py` مستقیماً قابل استفاده بود.
- کشف عملی: x.com ~۵۰٪ درخواست‌های تکی را 403 می‌دهد؛ Session کوکی‌گرفته بدتر (همیشه 403). راه‌حل: retry با session تازه — تست زنده ۱۰۰٪ موفق.
- `sources/xscrape.py`: `_fetch_script` (retry) + `fetch_tweet(url)` → (handle, entry نیتر-سازگار).
- `sources/twitter.py`: `build_tweet_item` (منطق مشترک از _fetch_xscrape استخراج شد) + `item_from_url` بدون فیلتر سن/طول/کلیدواژه.
- `main.py`: پیام «فقط لینک توییت» از ادمین → «در حال استخراج...» + thread پردازش + همان پیش‌نمایش و ۳ دکمه. تکراری → هشدار. فیلتر تکراری با db.is_duplicate.
- تست‌ها: ۱۰ تست شبکه‌-free جدید در tests/test_xscrape.py (۲۵ پاس در این فایل، کل suite: ۱۴۸ پاس).
- تست زنده سرتاسری: لینک واقعی Romano → item کامل با عکس → کپشن + ۳ دکمه.

## Decided
- لینک ادمین فیلترهای خبری عادی را رد می‌کند (سن/طول/کلیدواژه) چون ادمین خودش انتخابش کرده.
- فقط پیامِ «خالی» (فقط لینک، بدون متن دیگر) و فقط از ادمین پردازش می‌شود.

## Next
- تست واقعی روی گروه تلگرام (ربات باید روشن باشد).
- اگر خواستی غیرادمین‌ها هم بتوانند لینک بفرستند، شرط _is_admin برداشته شود.
